<Project Sdk="Microsoft.NET.Sdk">

  <!-- Framework -->
  <PropertyGroup>
    <TargetFramework>net6.0</TargetFramework>
  </PropertyGroup>

  <!-- Package -->
  <PropertyGroup>
    <Authors>ThomasWoodcock</Authors>
    <Description>FluentAssertion extensions for ThomasW.Domain.SharedKernel.Results</Description>
    <Copyright>Copyright Thomas Woodcock</Copyright>
    <PackageLicenseExpression>Apache-2.0</PackageLicenseExpression>
    <PackageProjectUrl>https://github.com/thomaswoodcock/ThomasW.Domain.SharedKernel.Results</PackageProjectUrl>
    <PackageTags>ThomasW.Domain.SharedKernel.Results;results;FluentAssertions;</PackageTags>
    <RepositoryUrl>https://github.com/thomaswoodcock/ThomasW.Domain.SharedKernel.Results.git</RepositoryUrl>
  </PropertyGroup>

  <!-- Publish -->
  <PropertyGroup>
    <EnablePackageValidation>true</EnablePackageValidation>
  </PropertyGroup>

  <!-- Compilation -->
  <PropertyGroup>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- Code analysis -->
  <PropertyGroup>
    <AnalysisMode>All</AnalysisMode>
    <EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>
  </PropertyGroup>

  <!-- Generated files -->
  <PropertyGroup>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <!-- References -->
  <ItemGroup>
    <ProjectReference Include="..\ThomasW.Domain.SharedKernel.Results\ThomasW.Domain.SharedKernel.Results.csproj" />
  </ItemGroup>
  <ItemGroup>
    <PackageReference Include="MinVer" Version="2.5.0" PrivateAssets="all"/>
    <PackageReference Include="FluentAssertions" Version="6.2.0" />
  </ItemGroup>

  <!-- MinVer -->
  <PropertyGroup>
    <MinVerTagPrefix>v</MinVerTagPrefix>
  </PropertyGroup>

</Project>
